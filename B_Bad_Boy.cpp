#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve(){
    int n, m, i, j;
    cin >> n >> m >> i >> j;

    cout << 1 << " " << m << " " << n << " " << 1 << endl;
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t; cin >> t;
    while(t--)
        solve();
    return 0;
}